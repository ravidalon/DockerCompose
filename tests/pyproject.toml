[project]
name = "dockercompose-tests"
version = "0.1.0"
description = "Integration tests for Docker Compose demo project"
requires-python = ">=3.11"

dependencies = [
    "pytest>=7.4.3",
    "requests>=2.31.0",
    "neo4j>=5.14.0",
]
