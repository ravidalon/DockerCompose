[project]
name = "database"
version = "0.1.0"
description = "Database service for Docker Compose demo"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.0",
    "neo4j>=5.14.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
