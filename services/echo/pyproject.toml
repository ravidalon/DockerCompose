[project]
name = "echo"
version = "0.1.0"
description = "Echo service for Docker Compose demo"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
